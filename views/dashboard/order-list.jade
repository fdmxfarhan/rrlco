.orders-list-item(id="orders-list-item-#{i}" class="#{order.state == 'تکمیل شده'? 'done' : ''} #{order.payed == false? 'notpayed':''}")
    .col= order.ownerNmae
    .col #{dateConvert.convertDate(order.date)}
    .col= order.phone
    .col= order.delivery
